.venv-drive/
docs/sessions/.last-drive-backup.json
*.redacted

# Python
__pycache__/
*.pyc
.venv/
pipeline/.venv/

# pipeline artifacts (videos, thumbs, previews) — but track the publish state
data/*
!data/published.json

# local blossom server runtime data
blossom-server/data/

# Node / web
node_modules/
web/node_modules/
web/.next/
web/data/

# secrets — never commit
.env
.env.*
